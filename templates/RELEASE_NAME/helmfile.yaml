bases:
  - ../../templates/env.yaml.gotmpl
---

## Repositories
{{- if .Values._set.repositories_sync }}
repositories: {{- .Values._set.repositories | toYaml | nindent 2 }}
{{- end }}

## Formulas
{{- $release := tpl (readFile "../../templates/release.tpl") (dict "context" $) -}}

## Releases
releases:
# renders the default release
- {{- $release | nindent 2 -}}
